node_modules
dist
out
*.vsix
.env
.env.local
.env.railway
.protege-store.json
.DS_Store
.turbo
coverage

# Large on-device model / native binary artifacts — downloaded at runtime
apps/extension/.model-cache/
apps/extension/models/
apps/extension/native/
apps/extension/bin/
*.gguf

# Wake-word training artifacts (23 GB: generated clips, features, checkpoints, ACAV100M)
voice/training/output/
voice/training/data/
voice-wav/

# Raw voice source recordings (user + ElevenLabs) — personal, not needed in repo
voice/*.m4a
voice/*.wav
voice/*.mp3
voice/elevanlabs/

# Per-developer handoff log written by Claude — see .claude/SKILLS/engineering.md
.claude/HANDOFF.md
